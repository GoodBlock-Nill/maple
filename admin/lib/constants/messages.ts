/**
 * 여러 화면이 함께 쓰는 고정 문구.
 *
 * 조회 실패는 지금까지 서버 로그에만 남고 화면에는 **빈 표**로 보였다. 운영자는
 * 그것을 "데이터가 없다"로 읽는다 — 신고가 0건이라고 믿고 넘어가는 사고가 난다.
 * 그래서 목록 조회가 깨진 화면은 표 위에 이 배너를 세운다.
 */
export const LIST_LOAD_ERROR =
  '목록을 불러오지 못했습니다. 표가 비어 보이는 것은 데이터가 없어서가 아닙니다. 새로고침해 주세요.'

/** 집계 질의가 깨졌을 때 지표 카드에 숫자 대신 넣는 값. */
export const COUNT_FAILED = '집계 실패'
