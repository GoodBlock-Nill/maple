# <메뉴명> — 화면·기능 설명

> 관리자 콘솔(`admin/`)의 <메뉴명> 메뉴. 경로 · 권한 · 화면 구성 · 동작(서버 액션) · 클라이언트(사용자 사이트)와의 상호작용을 개발 참고용으로 정리한다.
> 작성 규칙: 코드에서 확인한 사실만 쓴다. 파일 경로는 저장소 루트 기준. 워딩은 "카테고리"(말머리 금지).

## 0. 한눈에
| 항목 | 값 |
|---|---|
| 경로 | `/…` (하위: `/…/[id]`, …) |
| 권한 모듈 | `<module>` — read / write (`admin/lib/auth/permissions.ts`) |
| 주요 테이블 | `…` |
| 클라이언트 영향 | 캐시 태그 `…` 재검증 → 사용자 사이트 `/…` 즉시 반영 / 없음 |
| 관련 파일 | `admin/app/(admin)/…`, `admin/components/…`, `admin/lib/{actions,data,validation}/…` |

## 1. <화면 이름> (`/경로`)
**목적** 한 줄.

**화면 구성**
- 필터/탭: …
- 표 열: …
- 버튼/액션: …

**동작(서버 액션)**
| 동작 | 액션 함수 | 검증 | DB 변경 | 감사 로그 | 클라이언트 영향 |
|---|---|---|---|---|---|
| … | `…Action` (`admin/lib/actions/….ts`) | zod `…Schema` | `table.col` | `domain.verb` | 태그 `…` 재검증 |

**클라이언트와의 상호작용**
- 이 화면의 데이터가 사용자 사이트 어디에 어떻게 보이는지(경로·컴포넌트·조건).
- 사용자 사이트에서 들어오는 데이터(예: 문의 접수·신고)와 이 화면의 관계.

**주의**
- 제약(개수 한도·상태 전이·RLS·잠금 등) 한 줄씩.

## 2. <하위 화면> (`/경로/[id]`)
(같은 형식)
